# ─── Entorno ─────────────────────────────────────────────────────────────────
# Puerto en el que levanta Express (Render lo provee automáticamente)
PORT=3000

# ─── MercadoPago ─────────────────────────────────────────────────────────────
# Token de acceso MP (Sandbox: TEST-... | Producción: APP_USR-...)
MP_ACCESS_TOKEN=APP_USR-5086213774810453-021915-2ddd98574a2d4dc1286d0801eae28a38-3215166260

# ─── Expo Push Notifications ─────────────────────────────────────────────────
# Obtener en: https://expo.dev/accounts/<usuario>/settings/access-tokens
# Opcional — si no lo tenés, dejá vacío y los pushes usan el endpoint público
EXPO_ACCESS_TOKEN=

# ─── Firebase Admin SDK ───────────────────────────────────────────────────────
# Para Render: pegar el JSON del service account como string en una sola línea.
# Generarlo en: Firebase Console → Configuración → Cuentas de servicio → Generar clave privada
# IMPORTANTE: no usar comillas simples en Render, pegar el JSON directo en la variable
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"serviciosya-18f20",...}

# ─── Deep links ───────────────────────────────────────────────────────────────
# Scheme de la app para que MP redirija tras el pago
APP_SCHEME=oficiosya

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Orígenes permitidos (separar con coma si hay varios)
ALLOWED_ORIGINS=*
